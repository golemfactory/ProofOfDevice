import React from 'react';
import { home } from "./home.module.css";

const Home = () => {
    return (
        <div className={home}>
        	<span>Welcome to home :)</span>
        </div>
    );
};

export default Home;
